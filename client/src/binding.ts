// ui binding
const addressInput = document.getElementById("address-input") as HTMLInputElement;

const pingButton = document.getElementById("ping-button") as HTMLButtonElement;

const getInput = document.getElementById("get-input") as HTMLInputElement;
const getButton = document.getElementById("get-button") as HTMLButtonElement;

const setKeyInput = document.getElementById("set-key-input") as HTMLInputElement;
const setValueInput = document.getElementById("set-value-input") as HTMLInputElement;
const setButton = document.getElementById("set-button") as HTMLButtonElement;

const strlenInput = document.getElementById("strlen-input") as HTMLInputElement;
const strlenButton = document.getElementById("strlen-button") as HTMLButtonElement;

const delInput = document.getElementById("del-input") as HTMLInputElement;
const delButton = document.getElementById("del-button") as HTMLButtonElement;

const appendKeyInput = document.getElementById("append-key-input") as HTMLInputElement;
const appendValueInput = document.getElementById("append-value-input") as HTMLInputElement;
const appendButton = document.getElementById("append-button") as HTMLButtonElement;

const changeMembershipInput = document.getElementById("membership-input") as HTMLInputElement;
const changeMembershipButton = document.getElementById("membership-change-button") as HTMLButtonElement;

const logSection = document.getElementById("log-section") as HTMLDivElement;

export {
  addressInput,
  pingButton,
  getInput,
  getButton,
  setKeyInput,
  setValueInput,
  setButton,
  strlenInput,
  strlenButton,
  delInput,
  delButton,
  appendKeyInput,
  appendValueInput,
  appendButton,
  changeMembershipInput,
  changeMembershipButton,
  logSection,
}